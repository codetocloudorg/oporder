#!/usr/bin/env node
// Honest placeholder — no binary exists yet to download and install.
// This package exists only to reserve the name; it installs nothing.
// The `oporder` command (bin/oporder.js) prints this same message when
// actually run, since npm hides this postinstall output by default.
require("./lib/message")();
